---
title: "Testar Conexão SFTP da Campanha"
description: "Verificar a conectividade SFTP para uma campanha"
api: "POST /api/v1/campaigns/{campaign_id}/sftp/test-connection"
---

# Testar Conexão SFTP da Campanha

Executa uma verificação de conectividade contra o servidor SFTP configurado. O resultado é armazenado na configuração SFTP (`last_test_at`, `last_test_success`) e é necessário antes que a integração possa ser habilitada.

## Endpoint

```
POST /api/v1/campaigns/{campaign_id}/sftp/test-connection
```

## Parâmetros de caminho

<ParamField path="campaign_id" type="string" required>UUID da campanha.</ParamField>

## Cabeçalhos da requisição

<ParamField header="Authorization" type="string" required>
  Token Bearer. Formato: `Bearer talq_your_environment_token_here`
</ParamField>

## Exemplos

<RequestExample>

```bash
curl -X POST "https://app.talkover.ai/api/v1/campaigns/campaign-uuid/sftp/test-connection" \
  -H "Authorization: Bearer talq_your_environment_token_here"
```

</RequestExample>

## Resposta

### Sucesso

<ResponseExample>

```json
{
  "success": true,
  "message": "SFTP connection successful",
  "data": {
    "tested_at": "2024-01-15T16:45:00Z",
    "import_path_accessible": true,
    "export_path_accessible": true
  }
}
```

</ResponseExample>

### Falha

<ResponseExample>

```json
{
  "success": false,
  "message": "SFTP connection failed",
  "data": {
    "tested_at": "2024-01-15T16:45:00Z",
    "error": "Authentication failed"
  }
}
```

</ResponseExample>
