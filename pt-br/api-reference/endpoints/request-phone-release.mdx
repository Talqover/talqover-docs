---
title: "Solicitar Liberação de Número de Telefone"
description: "Enviar uma solicitação para liberar um número de telefone do seu ambiente"
api: "POST /api/v1/phone-numbers/{phone_number_id}/request-release"
---

# Solicitar Liberação de Número de Telefone

Envia uma solicitação de liberação para um número de telefone que você não precisa mais. A solicitação entra com status `pending` e é analisada antes que o número seja efetivamente liberado.

## Endpoint

```
POST /api/v1/phone-numbers/{phone_number_id}/request-release
```

## Parâmetros de caminho

<ParamField path="phone_number_id" type="string" required>
  UUID do número de telefone a ser liberado.
</ParamField>

## Cabeçalhos da requisição

<ParamField header="Authorization" type="string" required>
  Token Bearer. Formato: `Bearer talq_your_environment_token_here`
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Deve ser definido como `application/json`
</ParamField>

## Corpo da requisição

<ParamField body="reason" type="string" required>
  Motivo da liberação. Tamanho: 10–500 caracteres. Ajuda a equipe de suporte a analisar a solicitação mais rapidamente.
</ParamField>

## Exemplos

<RequestExample>

```bash
curl -X POST "https://app.talkover.ai/api/v1/phone-numbers/phone-uuid-1/request-release" \
  -H "Authorization: Bearer talq_your_environment_token_here" \
  -H "Content-Type: application/json" \
  -d '{
    "reason": "We migrated outbound calling to a new toll-free number and no longer need this DID."
  }'
```

```javascript
await fetch('https://app.talkover.ai/api/v1/phone-numbers/phone-uuid-1/request-release', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer talq_your_environment_token_here',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    reason: 'We migrated outbound calling to a new toll-free number and no longer need this DID.'
  })
});
```

</RequestExample>

## Resposta

### Resposta de Sucesso (201 Created)

<ResponseExample>

```json
{
  "success": true,
  "data": {
    "id": "request-uuid-1",
    "phone_number_id": "phone-uuid-1",
    "phone_number": "+15551234567",
    "reason": "We migrated outbound calling to a new toll-free number and no longer need this DID.",
    "status": "pending",
    "requested_at": "2024-01-15T12:00:00Z"
  }
}
```

</ResponseExample>

## Respostas de erro

### 409 — Já Pendente

<ResponseExample>

```json
{
  "success": false,
  "message": "A release request for this phone number is already pending",
  "code": "RELEASE_ALREADY_PENDING"
}
```

</ResponseExample>

### 422 — Número em Uso

<ResponseExample>

```json
{
  "success": false,
  "message": "Phone number is currently assigned to active agents and cannot be released",
  "code": "PHONE_NUMBER_IN_USE"
}
```

</ResponseExample>

## Observações

- Desvincule o número de quaisquer agentes (via [Atualizar Chamadas do Agente](/api-reference/endpoints/update-agent-calling)) antes de solicitar a liberação.
- Uma solicitação pendente pode ser cancelada com [Cancelar Solicitação de Liberação](/api-reference/endpoints/cancel-release-request).
- Uma vez aprovada, o número é liberado e **não pode ser recuperado**.
